#include "main.h"
#include <stdio.h>
/**
 * mul - a function that multiplies two integers
 * @a: first integer
 * @b: secend intager
 * Return: m the result
 */
int mul(int a, int b)
{
	m = a * b;
	return (m);
}
